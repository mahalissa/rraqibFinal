# RAQIB Server Dependencies
# --------------------------
# Install with:  pip install -r requirements.txt

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Deep learning – vehicle detection (YOLO11 + YOLOv8s-VisDrone via
# Ultralytics, Faster R-CNN via torchvision) + lane / drivable-area
# segmentation (YOLOPv2 TorchScript). Ultralytics pulls
# torch / torchvision / opencv / PIL / numpy transitively.
ultralytics>=8.3.0
torch>=2.2.0
torchvision>=0.17.0
Pillow>=10.0.0

# Image processing
opencv-python-headless>=4.9.0
numpy>=1.26.0

# Utilities
pydantic>=2.0.0
aiofiles>=23.0.0
httpx>=0.27.0
pusher>=3.3.0
huggingface_hub>=0.24.0
